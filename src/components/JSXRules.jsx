

const JSXRules = () => {
  return (
    <div>
        <h2>JSX Rules</h2>
        <p>JSX is a syntax extension for JavaScript that looks similar to XML or HTML. It is used with React to describe what the UI should look like.</p>
        <h3>Rules</h3>
        <ul>
            <li>JSX must have a single parent element.</li>
            <li>JSX elements must be properly closed.</li>
            <li>JSX elements can be nested inside each</li>
            </ul>
    </div>
  )
}

export default JSXRules;